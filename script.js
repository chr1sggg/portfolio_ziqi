// script.js
// No interactivity in the original design yet — this file is wired up
// and ready for you to add behavior (e.g. nav highlighting, animations,
// form handling, etc.) without touching the HTML or CSS.

document.addEventListener('DOMContentLoaded', () => {
  // Example: log that the page loaded, remove or replace as needed.
  console.log('Portfolio loaded.');
});
